# Scripts

This folder contains layer-specific ETL logic:

- **bronze** — landing tables and ingestion procedures  
- **silver** — data cleansing, validation, enrichment, intermediate modeling  
- **gold** — business modeling and dimensional views  
- **init** — database initialization and schema creation

Each folder contains reusable SQL scripts supporting the ETL pipeline for its respective layer.
